[project]
name = "{{ project_name }}-backend"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
]

[tool.uvicorn]
app = "app.main:app"

[tool.setuptools.packages.find]
include = ["app*"]
exclude = ["alembic*"]
